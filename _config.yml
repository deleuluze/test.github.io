title: deleuluze
email: riccardo.graziosi97@gmail.com
description: "i like writing and zines and critical theory and this is an attempt at all of these in a deleuzian assemblage sort of way"
baseurl: ""
url: "https://riggraz.dev"
github_username: riggraz

permalink: /:slug.html

favicon: "favicon.png"
goat_counter: "riggraz" # put your GoatCounter name if you want to use GoatCounter analytics

# Build settings
remote_theme: riggraz/no-style-please

theme_config:
  appearance: "light"
  back_home_text: "← back" # customize text for homepage link in post layout
  date_format: "%Y-%m" # customize how date is formatted in post list
  show_description: true # show blog description in home page
  lowercase_titles: true

plugins:
  - jekyll-feed
  - jekyll-seo-tag

include:
  - _pages

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
